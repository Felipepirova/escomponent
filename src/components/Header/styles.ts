import styled from 'styled-components'

export const Container = styled.div`
  background: var(--blue);

  div {
    max-width: 1120px;
    margin: 0 auto;
    padding: 2rem 1rem 4rem;

    h1 {
      font-weight: bold;
      color: var(--white);
    }
  }
`
